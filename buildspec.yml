version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Entered the post_build phase...
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/udccs-poc-0.0.3-SNAPSHOT.jar